# Cosmoria Configuration
# Copy to .env and adjust as needed.

DATABASE_URL=postgres://spiazzano:SI_giajLux42@localhost:5432/cosmoria?sslmode=disable
PORT=8080
AUTO_MIGRATE=true

# Optional: set these for production to persist tokens across restarts
 JWT_SECRET=your-256-bit-secret
ADMIN_JWT_SECRET=your-256-bit-admin-secret
        